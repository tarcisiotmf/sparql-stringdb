; Service custom parameters
;Protein identifiers are separated with %0d that is the URL encoding for a new line
; example: TP53%0dEGFR%0dCDK2 
custom_parameter[] = proteinIds
;custom_parameter[] = limit

; Web API query string
api_query = "https://string-db.org/api/json/interaction_partners?identifiers={proteinIds}&limit=0"

; Maximum time (in seconds) to cache responses from the Web API. Default: 2592000 = 30 days
cache_expires_after = 2592000

; Add provenance information to the graph generated at each invocation. Default: false
add_provenance = false
